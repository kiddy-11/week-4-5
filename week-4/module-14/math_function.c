#include <stdio.h>
#include <math.h>
#include<stdlib.h>
int main(){
    // double x;
    // scanf("%lf",&x);
    // int ans = ceil(x);

    // int ans = floor(x);

    // int ans = round(x);

    // int x;
    // scanf("%d",&x);
    // int ans = sqrt(x);

    // int a,b;
    // scanf("%d %d",&a,&b);
    // int ans = pow(a,b);
    // printf("%d\n",ans);

    
    int x;
    scanf("%d",&x);
    int ans = abs(x);
    printf("%d\n",ans);
    return 0;
}